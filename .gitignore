node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Base de données locale (Phase 3) — uniquement le dossier à la racine,
# pas src/data/ (contient mockData.ts, nécessaire au build).
/data/
